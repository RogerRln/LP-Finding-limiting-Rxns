For this example we are going to simulate Growth on CH (rich medium) using SMM (poor medium) as Baseline. We provide the following input data.

gr : growth rates vector
lb : Lower bound vector (Vmin * GeX_ratio)
ub : Upper bound vector (Vmax * GeX_ratio)
